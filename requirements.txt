# --- core retrieval ---
torch>=2.0
transformers>=4.38          # CLIP (retrieval) + BLIP ITM (reranking)
faiss-cpu>=1.7.4            # Kaggle ships faiss-gpu; use faiss-cpu locally
numpy>=1.24
pillow>=9.5
pandas>=2.0
tqdm>=4.65
matplotlib>=3.7
pyyaml>=6.0                 # config files
# --- serving / demo ---
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
gradio>=4.0                 # interactive demo UI
# --- dev ---
pytest>=7.4
# Note: on Kaggle, torch/transformers/faiss-gpu are preinstalled; you mainly need gradio.
